spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tech_blog_db?useSSL=false&serverTimezone=UTC
    username: root
    password: As2004125 # ???????????
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # ?????Entity??????????????
    show-sql: true # ??????SQL???????
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true # ??????SQL????

# ??????
server:
  port: 8080
